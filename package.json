{
  "name": "course-tool",
  "version": "0.1.0",
  "main": "main.js",
  "dependencies": {
    "electron": "^1.6.11",
    "jquery": "^3.2.1",
    "url-parse": "^1.1.9"
  },
  "scripts": {
    "start": "electron .",
    "macos": "electron-packager ./ CourseTool --platform=darwin  --arch=x64 --out ./dist",
    "win": "electron-packager ./ CourseTool --platform=win32  --arch=x64 --out ./dist",
    "test": "electron-packager ."
  },
  "devDependencies": {
    "electron-packager": "^8.7.2"
  }
}
